INSTRUCTIONS : 
Must install pip
open CMD , 
pip install wheel
pip install pandas
pip install matplotlib
pip install beautifulsoup4
pip install lmxl
pip install requests
pip install numpy


This is currently only for the 'most popular' tab on IMDB.
You can change sizing by dragging window borders.
This can be used as a template for other websites just replace results.requests.get(<webstie here>)
and replace the HTML tags in both nested for-loops.
